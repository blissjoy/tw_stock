# 股價表態仲裁元原則

- **Rule ID**: R-CHIP-14
- **名稱**: 股價表態仲裁元原則
- **分類**: 籌碼面
- **原文與頁碼**: `ai/ebook-summary-chen/P05-C3-破解主力手法狡兔三窟操控.md`（第5篇第3章「破解主力手法 狡兔三窟操控」，p.193-194）
- **解讀**: 書中評為「貫穿全書的元方法論」，跟其他規則不同，這不是一個「觸發即報」性質的訊號，而是一個**回測/驗證框架的核心原則**：當多個籌碼訊號互相矛盾時（例如主力買、投信賣），不糾結哪個訊號更可信，而是看事後股價實際走向，跟隨「贏的那一方」。中石化案例：市場派買、投信賣，結果股價跟著市場派漲，證明投信這次操作不具參考性；書中也提醒一種例外——公司籌碼不夠流通時，公司派可能透過交易轉帳給市場派，雙方聯手作價，此時「大咖對大咖」的矛盾其實是假矛盾。這個原則應該作為本專案設計任何籌碼類規則回測驗證流程的核心方法論：訊號的可信度應該用「歷史上跟隨這個訊號的勝率」來衡量，而不是預設某類法人/主力天生更可信。
- **可程式化**: 部分——這不是一個可以獨立觸發的訊號規則，而是**應該內建進所有籌碼規則的回測評分邏輯裡**的方法論：當兩個或以上訊號同時出現且方向相反時，用「事後N天股價報酬率」判定哪個訊號「贏了」，並據此調整該訊號來源在類似情境下的可信權重。這需要先有多個獨立訊號來源（機構買賣超、主力券商動向等）才能真正發揮作用，目前本專案的`analyze_chip_signals()`（`src/presentation/stock_detail_data.py`）已經同時顯示多條籌碼訊號，但還沒有「訊號矛盾時看事後股價表態」這種自動仲裁/加權機制。
- **所需資料**: 多個獨立籌碼訊號來源（現有的三大法人、融資、量縮等訊號已有部分）、股價（已有）。
- **計算公式**:
```
function signal_arbitration(signal_a_direction, signal_b_direction, stock, t, horizon_days=10):
    if signal_a_direction == signal_b_direction:
        return None  # 訊號一致，不需要仲裁
    future_return = (close[t + horizon_days] - close[t]) / close[t]
    winner = signal_a_direction if (future_return > 0) == (signal_a_direction == "buy") else signal_b_direction
    return winner  # 事後判定哪一方訊號較準確，可用於累積該訊號來源的歷史準確度統計
```
- **參數**: 事後驗證的觀察窗口(`horizon_days`)書中沒有給精確天數，需自行決定並回測。
- **可回測**: 是——這個原則本身不需要新資料源，只需要既有的多個訊號+股價資料即可回測「矛盾情境下跟隨哪一方訊號的歷史勝率較高」，是本清單中少數不受`broker_chips`瓶頸限制、隨時可以開始實作的項目之一，但目前還沒有人寫這段程式碼。
- **信心**: 60/100 中（方法論的普適性與重要性在書中被明確強調為核心原則，且不依賴任何新資料源，理應可以較快實作；扣分原因是這不是一條「規則」而是一個「回測框架設計原則」，需要先決定要用哪些既有訊號來源組合仲裁，目前還停留在概念層次，沒有具體的實作規劃）。

## 實作進度

2026-08-09第一階段實作：`src/indicators/signal_backtest.py`新增`forward_return_
stats()`(給定觸發日期清單+收盤價序列+觀察天數，算出勝率/平均報酬率，樣本不足時
n=0且win_rate/avg_return為None，不是0)跟`arbitrate()`(兩個方向相反的訊號依事後
報酬率判定誰贏)，`tests/test_signal_backtest.py`7個測試全過，但當時只做了通用
回測工具本身，還沒有接上即時UI。

**2026-08-09第二階段：即時訊號衝突偵測正式接上**。`src/presentation/stock_
detail_data.py`新增：
- `_signal_direction(rule_id, note)`：把scan_chip_tier()既有5條規則(R-SCREEN-06/
  R-CHIP-01/02/03/07)標上「買/賣」方向標籤，R-CHIP-02/07的方向依note文字關鍵字
  （斷頭/超跌、續漲/續跌）判斷，因為同一個rule_id可能代表相反方向。
- `_historical_signal_trigger_dates(conn, stock_id, rule_id, direction)`：重建
  該規則在這檔股票**完整歷史**上的觸發日期清單，供回測用。R-CHIP-02/03沿用既有
  的向量化函式(`margin_oversold_rebound_signal()`／`volume_washout_signal()`
  本來就回傳整段歷史的Series)；R-SCREEN-06/R-CHIP-01新增`src/indicators/
  institutional_flow.py`的`flow_streak_series()`向量化版本(避免對長歷史逐日
  呼叫`classify_flow_streak()`造成O(n²)開銷，用`classify_flow_streak()`本身
  當ground truth回歸測試驗證向量化結果一致，見`tests/test_institutional_
  flow.py`3個新測試)；R-CHIP-07用holder_shares_distribution的公告日期逐對
  比較(週更新，資料量小，直接迴圈即可)。
- `detect_chip_signal_conflict(conn, stock_id, scan_results)`：偵測「今天」是否
  同時出現買/賣方向訊號，若有，對雙方各自呼叫`forward_return_stats()`算出這檔
  股票歷史上的觸發後10個交易日報酬率統計。⚠️修正一個方向性bug：`forward_return_
  stats()`的`win_rate`定義固定是「股價上漲比例」，賣訊號要「準」代表股價要跌，
  賣方向的win_rate要取`1 - 原始值`，不能直接沿用（第一版寫錯，測試才抓到）。
  ⚠️另外排除「矛盾雙方剛好是同一個rule_id」的假矛盾（例如R-CHIP-02的斷頭警示vs
  超跌反彈，這兩者是同一份融資維持率資料在不同時間窗口的解讀，超跌反彈成立時
  斷頭警示邏輯上必然也成立，不是「不同來源意見不合」，不符合書中「主力買vs投信
  賣」這種矛盾的原意)——只在能找到rule_id不同的買/賣配對時才判定為矛盾。
- 接進`scan_chip_tier()`：矛盾存在時額外附加一筆R-CHIP-14的note，例如「訊號
  矛盾：R-CHIP-03(買方向，本股歷史勝率39%，n=297) vs R-SCREEN-06(賣方向，本股
  歷史勝率61%，n=157)同時觸發，僅供歷史參考，不保證這次結果相同」。

⚠️這是「歷史統計」不是「未卜先知」，note文字明確標註「僅供參考，不保證這次結果
相同」，不能讓使用者誤以為系統已經幫忙解決矛盾。

真實驗證：`pytest tests/ -q`1113個測試全過(新增`tests/test_institutional_
flow.py`3個、`tests/test_stock_detail_data.py`5個，含一個「同rule_id假矛盾」
的回歸測試)。對正式`data/tw_stock.db`真實300檔股票跑`scan_chip_tier()`，0個
crash，28檔觸發R-CHIP-14矛盾偵測(9.3%)，輸出文字人工檢視內容合理。
