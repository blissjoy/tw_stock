pandas>=2.2
numpy>=1.26
pytest>=8.0
requests>=2.31
# Turso(libsql)雲端資料庫連線：官方文件推薦的新版`libsql`套件是Rust原生binding，在部分環境
# (例如較新的Python版本)沒有預編譯wheel、需要Rust+cmake從原始碼編譯而安裝失敗；改用純Python
# 實作、實測可正常安裝運作的舊版`libsql-client`。若之後這個套件停止維護，只需要改
# src/data/turso_client.py 這一個檔案。
libsql-client
streamlit>=1.35
plotly>=5.20

# PySide6桌面版(desktop/)：2026-07-23架構調整，改回本機優先運作，不再強制依賴Turso/Streamlit
# Cloud。本機已驗證6.11.1可正常運作，含QtWebEngine(desktop/main_window.py用QWebEngineView
# 顯示Plotly圖表，重用跟Streamlit版相同的src/presentation/chart_data.py畫圖邏輯)。
PySide6>=6.6.0

# TPEx股價批次下載(src/data/yfinance_client.py)：2026-07-23改用yfinance取代FinMind逐股抓取，
# 仿照ref-project/tw_stock_analyzer長期實測驗證過的yf.download(tickers_array)批次做法，
# 原本約需1小時的TPEx更新縮短到數十秒內。本機已驗證1.4.1可正常運作。
yfinance>=1.4.1

# 「個股分析」面板「原文與頁碼」連結開新視窗閱讀ai/ebook-summary/筆記檔案時，把.md轉成
# HTML顯示用(desktop/main_window.py的_open_rule_reference_window())。純Python、無C
# 擴充套件，本機已驗證3.10.3可正常運作。
markdown>=3.7

# 觀察清單自動匯出Google Sheet(src/data/google_sheets_client.py)：2026-08-04新增。
# gspread包一層Sheets API操作介面(讀寫儲存格、格式化、建立分頁)；google-auth/
# google-auth-oauthlib負責OAuth Desktop app授權流程(使用者的Google Cloud專案關閉了
# 服務帳戶金鑰建立功能，改用OAuth使用者登入，見config.py的get_google_oauth_client_
# config()說明)。
gspread>=6.1
google-auth>=2.32
google-auth-oauthlib>=1.2

# 個股報表PDF匯出(dashboard/app.py)：2026-08-05新增。weasyprint把組好的HTML字串轉PDF，
# 選它不選Playwright headless Chromium是因為部署到Streamlit Community Cloud的負擔輕很多
# (只需要透過packages.txt裝系統層Pango/Cairo/GDK-Pixbuf函式庫，不用整包~300MB的Chromium
# 執行檔)；代價是weasyprint不執行JavaScript，圖表不能用chart_render.py那份互動HTML，改用
# kaleido(Plotly官方推薦的靜態圖匯出引擎，fig.to_image()會自動偵測使用，不需要額外import)
# 把Figure轉成PNG再嵌入報表HTML。weasyprint在Windows本機安裝需要另外裝GTK3執行期函式庫
# (pip本身不會帶，見README)，Streamlit Cloud的Debian容器透過packages.txt裝系統套件即可。
weasyprint>=62.0
kaleido>=0.2.1
