ebook/
ref-project/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Secrets
.env
.env.*
!.env.example

# Local scratch databases (production db path/persistence strategy decided separately)
*.db
*.sqlite
*.sqlite3

# Local script run logs (e.g. scripts/backfill_history.py stdout redirected to a file)
*.log
backfill_log.txt

# 每日pipeline執行狀態(供PySide6桌面版UI輪詢顯示「目前正在自動跑」)，純本機執行期狀態，不需要進版控
data/pipeline_status.json
# 每次pipeline執行的追加式診斷紀錄(見src/presentation/pipeline_status.py的
# append_run_snapshot())，純本機事後診斷用途，會持續增長，不需要進版控
data/pipeline_run_history.jsonl

# Google Sheets匯出(src/data/google_sheets_client.py)的OAuth授權後token快取——程式自己
# 讀寫、內容會隨token刷新不斷改變，不是人手動維護的設定(那些放在.env)，純本機憑證，不進版控
data/google_oauth_token.json

temp/