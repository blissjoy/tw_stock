ebook/
ref-project/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Secrets
.env
.env.*
!.env.example

# Local scratch databases (production db path/persistence strategy decided separately)
*.db
*.sqlite
*.sqlite3

# Local script run logs (e.g. scripts/backfill_history.py stdout redirected to a file)
*.log
backfill_log.txt

# 每日pipeline執行狀態(供PySide6桌面版UI輪詢顯示「目前正在自動跑」)，純本機執行期狀態，不需要進版控
data/pipeline_status.json
